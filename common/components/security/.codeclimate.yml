engines:
  duplication:
    enabled: true
    config:
      languages:
      - php
  fixme:
    enabled: true
  phpmd:
    enabled: true
    config:
      rulesets: "codesize,dashboard,unusedcode"
ratings:
  paths:
  - "**.php"
exclude_paths:
- tests/